# TODO

- [ ] Future: allow `/send` posts to attach to an existing event instead of always creating a new event.
  - Short UI wording: `Привязать к событию`.
  - First event post creates the event; later posts can select it from an inline menu.

